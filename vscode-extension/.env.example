# VSCode Extension Configuration
# No API keys needed - extension uses secure backend API

# Backend API URL (where the main app is running)
BACKEND_API_URL=http://localhost:3000

# Extension will authenticate through the main app
# No sensitive credentials stored in extension